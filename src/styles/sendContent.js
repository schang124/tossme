import styled from 'styled-components';

const side = '25px';

export const Wrapper = styled.div`
    margin: 0 ${side};
`;

export const Title = styled.h1`
    font-size: 12px;
    color: white;
    opacity: 0.5;
`;